const { name, department, app, my_name } = require('./modules.js');

console.log(name);
console.log(department);

console.log(`This is ${my_name}`);

//can be imported into a single variable and data members can also be accessed via . operator as well
